# LegixTech

App de monitoramento legislativo — **React Native + Expo + NativeWind**.

O projeto web (Vite/React DOM) foi migrado para a pasta `mobile-app/`.

## Como rodar

```bash
cd mobile-app
npm install
npx expo start
```

Veja [mobile-app/README.md](mobile-app/README.md) para mais detalhes.
